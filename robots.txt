# robots.txt for https://www.jtubert.com/
#
# Everything is open to every crawler, including AI search and answer engines,
# which are named below so the intent is explicit rather than inherited from *.
# They share one group on purpose: a crawler that matches a named group ignores
# the * group entirely, so separate groups would each need the /test/ rule
# repeated or it would silently stop applying to them.
#
# Disallow comes before Allow deliberately. Google takes the longest matching
# rule, so order does not matter to it, but parsers that take the first match
# would read "Allow: /" and never reach /test/. This order is right under both.

User-agent: *
User-agent: Googlebot
User-agent: Google-Extended
User-agent: Bingbot
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-SearchBot
User-agent: Claude-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Applebot
User-agent: Applebot-Extended
User-agent: CCBot
User-agent: DuckAssistBot
User-agent: Meta-ExternalAgent
User-agent: Amazonbot
Disallow: /test/
Allow: /

Sitemap: https://www.jtubert.com/sitemap.xml
